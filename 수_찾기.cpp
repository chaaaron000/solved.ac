#include <iostream>
#include <vector>
#include <string>
#include <cmath>
#include <algorithm>

using namespace std;

bool bs(vector<int> &v, int n)
{
    int i = v.size() - 1;

    while (true)

    return false;
}

void Solution()
{
    int n, m;
    cin >> n;
    vector<int> v(n);
    for (int i = 0; i < n; i++)
        cin >> v[i];

    sort(v.begin(), v.end());

    cin >> m;
}

int main(int argc, char const *argv[])
{
    Solution();

    return 0;
}
